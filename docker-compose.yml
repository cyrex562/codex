version: '3.8'

services:
  codex:
    build: .
    image: codex:latest
    container_name: codex
    ports:
      - "8080:8080"
    volumes:
      - codex_data:/data
      # Uncomment to mount local vault directory:
      # - ./my-vaults:/data/vaults
    environment:
      - CODEX__SERVER__HOST=0.0.0.0
      - CODEX__SERVER__PORT=8080
      - CODEX__DATABASE__PATH=/data/codex.db
      - CODEX__VAULT__BASE_DIR=/data/vaults
      - CODEX__AUTH__ENABLED=true
      - CODEX__AUTH__BOOTSTRAP_ADMIN_USERNAME=admin
      # IMPORTANT: Change this before first run, then remove it.
      - CODEX__AUTH__BOOTSTRAP_ADMIN_PASSWORD=changeme-on-first-login
      # Generate a stable secret: openssl rand -hex 32
      # - CODEX__AUTH__JWT_SECRET=your-secret-here
      # Override config file path (default: /data/config.toml via CMD --config)
      # - CODEX_CONFIG=/data/config.toml
      - RUST_LOG=info
      # Rate limiting (requests per 60-second window per IP)
      - RATE_LIMIT_REQUESTS=120
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

volumes:
  codex_data:
